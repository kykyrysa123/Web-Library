# Лабораторная работа №6: Улучшение UX
## Web Library Application (React Frontend)

---

**Атрибуты качества ПО группы “Usability” (согласно ISO/IEC 25010:2011)**

1. **Распознаваемость соответствия:** Степень, в которой пользователи могут распознать, подходит ли продукт для их нужд на основе первоначальной документации, демонстрации или информации.
2. **Обучаемость:** Степень, в которой продукт позволяет пользователям эффективно и результативно обучаться его использованию.
3. **Используемость (операбельность):** Степень, в которой продукт может быть использован для достижения целей с эффективностью, производительностью и удовлетворением в указанном контексте использования.
4. **Защита от ошибок пользователя:** Степень, в которой продукт защищает пользователей от совершения ошибок.
5. **Эстетика GUI:** Степень, в которой интерфейс пользователя обеспечивает приятное и удовлетворяющее взаимодействие.
6. **Доступность:** Степень, в которой продукт может быть использован людьми с самыми широкими возможностями и ограничениями для достижения целей.

**Требования и стандарты**

- Microsoft Usability Guidelines
- Apple Human Interface Guidelines
- Google Material Design
- WCAG 2.0 (для web-приложений)

**Уровни “Опыта использования” (по модели Джесса Гарретта)**

1. **Уровень поверхности:** Внешний вид продукта (текст, изображения, кнопки и т.д.).
2. **Уровень компоновки:** Конкретная реализация структуры (эффективное расположение элементов UI).
3. **Уровень структуры:** Взаимное расположение страниц, форм, окон (навигация: "откуда", "куда", "как").
4. **Уровень набора возможностей:** Перечисление функциональных возможностей.
5. **Уровень стратегии:** Абстрактный уровень желаний и ожиданий пользователей и заказчика.

---

## 1. Оценка разрабатываемого ПО по атрибутам

Разрабатываемое ПО — **Web Library Application** (веб-приложение для управления библиотекой на React с Material-UI). Оценка проводится на основе анализа предоставленного кода компонентов.

| Атрибут | Оценка | Обоснование |
|---------|--------|-------------|
| **Распознаваемость соответствия** | ⭐⭐⭐⭐⭐⭐☆☆☆☆ (6/10) | Навигационная панель с разделами "Книги", "Авторы", "Избранное" помогает пользователю понять функционал. |
| **Обучаемость** | ⭐⭐⭐⭐⭐⭐☆☆☆☆ (6/10) | Интерфейсы интуитивны, формы содержат подсказки, но отсутствует подробный onboarding. |
| **Используемость (операбельность)** | ⭐⭐⭐⭐⭐⭐⭐⭐☆☆ (8/10) | Улучшенная навигация, поиск по книгам и авторам, пагинация, вкладка "Избранное" значительно повышают удобство использования. |
| **Защита от ошибок пользователя** | ⭐⭐⭐⭐⭐⭐⭐☆☆☆ (7/10) | Добавлено подтверждение пароля при регистрации, подсказки в формах. Диалоги подтверждения удаления. |
| **Эстетика GUI** | ⭐⭐⭐⭐⭐⭐⭐⭐☆☆ (8/10) | Современный дизайн с Material-UI, индикаторы жизни авторов (зеленый/красный), сердечки для избранного, улучшенная пагинация. |
| **Доступность** | ⭐⭐⭐⭐☆☆☆☆☆☆ (4/10) | Используются стандартные компоненты Material-UI с базовой доступностью, но требуется доработка ARIA-атрибутов. |

---

## 2. Выбор путей улучшения UX, обоснование необходимости

На основе оценки и уровней UX (Garrett), выбраны улучшения, фокусируясь на уровнях поверхности, компоновки и структуры.

| № | Улучшение | Уровень UX | Обоснование | Реализация в коде |
|---|-----------|------------|-------------|-------------------|
| 1 | Добавить вкладку "Избранное" на главной странице | Структуры | Позволяет пользователям быстро доступаться к любимым книгам | `Home.js`, `BooksList.js`, `AuthorsList.js` |
| 2 | Добавить кнопку "Очистить" у поиска | Поверхности | Упрощает сброс поискового запроса | `Home.js`, `BooksList.js`, `AuthorsList.js` |
| 3 | Реализовать поиск по книгам и авторам | Структуры | Значительно улучшает находимость контента | `Home.js`, `BooksList.js`, `AuthorsList.js` |
| 4 | Улучшить пагинацию | Компоновки | Делает навигацию по страницам более удобной | Все компоненты со списками |
| 5 | Добавить подсказки на окне регистрации | Поверхности | Помогает пользователям правильно заполнять форму | `RegisterPage.js` |
| 6 | Добавить подтверждение пароля при регистрации | Компоновки | Защита от ошибок (опечаток) | `RegisterPage.js` |
| 7 | Добавить подсказки на окне входа | Поверхности | Помогает пользователям вспомнить требования к полям | `LoginPage.js` |
| 8 | Добавить сердечки на изображения книг для избранного | Поверхности | Визуальный индикатор для добавления в избранное | `BooksList.js`, `FavoriteButton` |
| 9 | Добавить индикаторы жизни авторов (зеленый/красный) | Поверхности | Визуально показывает, жив ли автор | `AuthorsList.js` |
| 10 | Добавить выпадающий список книг автора | Структуры | Упрощает просмотр книг конкретного автора | `AuthorsList.js` с Collapse |

---

## 3. Проведенные улучшения

### Улучшение 1-2: Вкладка "Избранное" на главной странице и кнопка "Очистить" у поиска

**До:** На главной странице отсутствовала вкладка избранного, поиск без кнопки очистки.

**После:** Добавлена вкладка "Избранное" со счетчиком и кнопка очистки поиска.

```jsx
// Home.js - навигационная панель с избранным
<AppBar position="static" sx={{ backgroundColor: '#8B4513', mb: 4 }}>
    <Toolbar>
        <Typography variant="h6" sx={{ flexGrow: 1 }}>
            Библиотека
        </Typography>
        <Button color="inherit" component={Link} to="/books">
            Книги
        </Button>
        <Button color="inherit" component={Link} to="/authors">
            Авторы
        </Button>
        {/* Кнопка Избранное с счетчиком */}
        <Button color="inherit" component={Link} to="/favorites">
            Избранное
        </Button>
    </Toolbar>
</AppBar>

// Кнопка очистки поиска
<Button
    variant="outlined"
    onClick={handleClear}
    sx={{ borderColor: '#8B4513', color: '#8B4513' }}
>
    Очистить
</Button>
```

### Улучшение 3: Поиск по книгам и авторам

**До:** Отсутствовал поиск, пользователи не могли найти конкретные книги или авторов.

**После:**  Реализован поиск по книгам и авторам.

```jsx
// Home.js - поиск с выбором типа
<Box sx={{ mb: 4, display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 2 }}>
    <FormControl component="fieldset">
        <RadioGroup
            row
            value={searchType}
            onChange={(e) => setSearchType(e.target.value)}
        >
            <FormControlLabel value="author" control={<Radio />} label="По автору" />
            <FormControlLabel value="title" control={<Radio />} label="По названию" />
        </RadioGroup>
    </FormControl>

    <Box>
        <TextField/>
        <Button>
            Найти
        </Button>
        <Button
            variant="outlined"
            onClick={handleClear}
            sx={{ borderColor: '#8B4513', color: '#8B4513' }}
        >
            Очистить
        </Button>
    </Box>
</Box>

// Функция поиска
const handleSearch = async () => {
    if (!searchQuery.trim()) {
        setBooks(allBooks);
        setPage(1);
        return;
    }
    const query = searchQuery.trim().toLowerCase();
    try {
        if (searchType === 'author') {
            const filtered = allBooks.filter((book) =>
                book.authors &&
                Array.isArray(book.authors) &&
                book.authors.some((author) =>
                    author && author.name && author.surname
                        ? `${author.surname} ${author.name}${author.patronymic ? ' ' + author.patronymic : ''}`
                            .toLowerCase()
                            .includes(query)
                        : false
                )
            );
            setBooks(filtered);
        setPage(1);
    } catch (error) {
        console.error('Ошибка поиска:', error);
        setBooks([]);
    }
};
```

### Улучшение 4-5: Подсказки и подтверждение пароля при регистрации

**До:** Простая форма регистрации без подсказок и подтверждения пароля.

**После:**  Добавлены подсказки и подтверждение пароля с валидацией.

```jsx
// RegisterPage.js - форма с валидацией
const validateForm = () => {
    if (!userData.username || !userData.email || !userData.password || !userData.confirmPassword) {
        setLocalError('Пожалуйста, заполните все поля');
        return false;
    }

    if (userData.password.length < 6) {
        setLocalError('Пароль должен содержать минимум 6 символов');
        return false;
    }

    if (userData.password !== userData.confirmPassword) {
        setLocalError('Пароли не совпадают');
        return false;
    }

    const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    if (!emailRegex.test(userData.email)) {
        setLocalError('Введите корректный email адрес');
        return false;
    }

    return true;
};

// Поля с подсказками
<input
    type="password"
    name="password"
    value={userData.password}
    onChange={handleChange}
    style={styles.input}
    placeholder="Минимум 6 символов"
    disabled={isLoading}
/>

<input
    type="password"
    name="confirmPassword"
    value={userData.confirmPassword}
    onChange={handleChange}
    style={styles.input}
    placeholder="Повторите пароль"
    disabled={isLoading}
/>
```

### Улучшение 6: Сердечки для добавления в избранное

**До:** На странице книг отсутствовала возможность добавлять книги в избранное.

**После:**  Добавлены сердечки на изображения книг.

```jsx
// BooksList.js - компонент с избранным
<Card sx={{ position: 'relative' }}>
    <CardActionArea onClick={() => handleCardClick(book.id)}>
        <CardMedia
            component="img"
            height="140"
            image={book.imageUrl || 'https://via.placeholder.com/140'}
            alt={book.title}
            sx={{ objectFit: 'cover' }}
        />
        {/* Остальной контент */}
    </CardActionArea>

    {/* Кнопка избранного */}
    <Box sx={{ position: 'absolute', top: 8, right: 8 }}>
        <FavoriteButton
            book={book}
            size="large"
            sx={{
                backgroundColor: 'rgba(255, 255, 255, 0.8)',
                '&:hover': {
                    backgroundColor: 'rgba(255, 255, 255, 0.9)'
                }
            }}
        />
    </Box>
</Card>

// Контекст избранного для счетчика
const { favorites } = useFavorites();
```
4. Картинки/описания “до” vs. “after”

Главная страница:

До: Отсутствовала вкладка "Избранное", поиск без кнопки очистки

![Главная страница до](image.png)

После: Добавлена вкладка "Избранное" со счетчиком, кнопка очистки поиска

![Главная страница после](image-1.png)

Страница книг:

До: Простой список книг без возможности добавить в избранное

![Страница книг до](image-4.png)

После: Добавлены сердечки на изображениях для добавления в избранное, улучшенная пагинация

![Страница книг после](image-5.png)

Страница авторов:

До: Только информация об авторе

![Страница авторов до](image-2.png)

После: Добавлены индикаторы жизни (зеленый/красный), выпадающий список книг

![Страница авторов после](image-3.png)

Форма регистрации:

До: Простая форма без подсказок и подтверждения

![Форма регистрации до](image-8.png)

После: Добавлены подсказки для всех полей, подтверждение пароля с валидацией

![Форма регистрации после](image-9.png)

Форма входа:

До: Только поля для ввода

![Форма входа до](image-6.png)

После: 	Добавлены подсказки и обработка ошибок

![Форма входа после](image-7.png)

Страница Избранное:

![Страница избранное](image-10.png)

## 5. Заключение

Улучшения, внесенные в процессе работы, позволили значительно повысить показатели usability по всем атрибутам качества. В таблице ниже представлена динамика изменений:

| Атрибут | До | После |
|---------|:--:|:-----:|
| **Распознаваемость соответствия** | 6/10 | 8/10 |
| **Обучаемость** | 6/10 | 8/10 |
| **Используемость (операбельность)** | 8/10 | 9/10 |
| **Защита от ошибок пользователя** | 7/10 | 9/10 |
| **Эстетика GUI** | 8/10 | 9/10 |
| **Доступность** | 4/10 | 6/10 |

---

### Полный список реализованных улучшений

#### 🎯 Навигация и структура
- ✅ **Вкладка "Избранное"** на главной странице с динамическим счетчиком (`favorites.length > 0`)
- ✅ **Вкладка "Избранное"** с любимыми книгами пользователя (контекст `favorites`)
- ✅ **Выпадающий список книг автора** (компонент Material-UI `Collapse`)
- ✅ **Улучшенная пагинация** с номерами страниц (Material-UI `Pagination`)

#### 🔍 Поиск и фильтрация
- ✅ **Кнопка "Очистить"** у поиска (функция `handleClear`)
- ✅ **Поиск по книгам и авторам** с радиокнопками выбора типа поиска
- ✅ **Поиск на страницах книг и авторов** (состояние `searchQuery`)

#### 🛡️ Защита от ошибок
- ✅ **Подтверждение пароля** при регистрации (поле `confirmPassword`)
- ✅ **Диалоги подтверждения** для операций удаления (`window.confirm` в `handleDeleteBook`)

#### 💡 Обучаемость и подсказки
- ✅ **Подсказки на окне регистрации** (`placeholder` с пояснениями)
- ✅ **Подсказки на окне входа** (обработка ошибок)

#### 🎨 Визуальные улучшения
- ✅ **Сердечки на изображениях книг** для добавления в избранное (компонент `FavoriteButton`)
- ✅ **Индикаторы жизни авторов** (зеленый/красный кружок)
- ✅ **Цветовая схема в стиле библиотеки** (коричневые тона `#8B4513`)
- ✅ **Адаптивный дизайн** (Material-UI `Grid` система)

---

### Вывод

Проделанная работа позволила создать более **интуитивно понятное**, **доступное** и **визуально приятное** веб-приложение. Пользователи теперь могут:

- Легко находить книги и авторов через **улучшенный поиск**
- Добавлять книги в **избранное** и быстро возвращаться к ним
- Видеть **статус авторов** и просматривать их книги без дополнительных переходов
- Получать **понятные подсказки** при регистрации и входе
- Быть **защищенными от случайных действий** благодаря диалогам подтверждения

Улучшенная пагинация и адаптивный дизайн делают использование приложения комфортным на любых устройствах, а единая цветовая схема создает приятную атмосферу, соответствующую тематике библиотеки.